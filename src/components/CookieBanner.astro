---
/**
 * Cookie notice — honest, local-only. Preference stored in localStorage.
 * Muncul dengan delay agar tidak menimpa CTA hero saat first paint.
 */
---

<div id="cookie-banner" hidden class="fixed bottom-3 left-3 right-3 z-50 sm:right-auto sm:max-w-xs" role="region" aria-label="Pemberitahuan cookie">
  <div class="flex items-start gap-3 rounded-xl border border-foreground/20 bg-card px-4 py-3 shadow-lg" style="padding-bottom:calc(0.75rem + env(safe-area-inset-bottom, 0px));">
    <p class="flex-1 text-xs leading-relaxed text-muted-foreground">
      Tanpa pelacakan — hanya preferensi tema, disimpan lokal.
      <a href="/privacy" class="text-secondary p-2 -m-2 inline-block underline underline-offset-2">Privasi</a>.
    </p>
    <button id="cookie-ok" type="button" class="btn-primary shrink-0 !min-h-11 !px-4 text-xs">OK</button>
  </div>
</div>

<script>
  (function () {
    try {
      if (!localStorage.getItem('mm-cookie-ok')) {
        // Delay: beri waktu pengguna melihat konten/CTA dulu, hindari menimpa CTA hero
        setTimeout(() => {
          document.getElementById('cookie-banner')?.removeAttribute('hidden');
        }, 1500);
      }
      document.getElementById('cookie-ok')?.addEventListener('click', () => {
        localStorage.setItem('mm-cookie-ok', '1');
        document.getElementById('cookie-banner')?.setAttribute('hidden', '');
      });
    } catch {}
  })();
</script>
